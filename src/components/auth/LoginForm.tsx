'use client';

// This component is no longer used as the login page has been removed.
// Keeping the file to avoid breaking potential (though unlikely) direct imports,
// but it will render nothing.
export default function LoginForm() {
  return null;
}
